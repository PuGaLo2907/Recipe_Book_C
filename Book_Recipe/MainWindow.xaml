<Window x:Class="Book_Recipe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Book_Recipe"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Title="Книга рецептов" Height="1080" Width="1920" Icon="Image_glav/Glav_Image.jpg">
    <Grid Background="#8ee4af">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#8ee4af">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Button Name="btn_Back" Click="btn_Click" Content="Назад"  Grid.Column="1" Style="{StaticResource RoundedButton}">
                <Button.Resources>

                </Button.Resources>
            </Button>
           
        </Grid>
        <Frame ContentRendered="frameRender" NavigationUIVisibility="Hidden" Name="frame" Grid.Row="1" Background="#FFEDF5E1"/>
    </Grid>
</Window>
